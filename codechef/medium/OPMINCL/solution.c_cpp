class Solution {
public:
    int countNonMinimum(vector<int>& nums) {
        int mn = *std::min_element(nums.begin(), nums.end());
        
        int cnt = 0;
        for(int x : nums) {
            if(x == mn) cnt++;
        }
        
        return nums.size() - cnt;
        
    }
};

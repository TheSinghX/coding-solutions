#include <bits/stdc++.h>
using namespace std;

int main() {
	int x, y, f;
	cin >> x >> y >> f;
	
	for(int i=0;i<n;i++){
	    cin >> a[i];
	    
	    if(X=max){
	        cout << 12*"x";
	        
	        else if(y=max){
	            cout << 12*"y"+f;
	        }
	    }
	}
	return 0;
}
